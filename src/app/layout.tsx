import type { Metadata } from "next";
import { Geist, Geist_Mono, Newsreader } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const newsreader = Newsreader({
  variable: "--font-newsreader",
  subsets: ["latin"],
  style: ["normal", "italic"],
});

export const metadata: Metadata = {
  title: "Sparkboard — play-money prediction markets",
  description:
    "Open-source play-money prediction game. Anyone can open a market. LMSR prices. Friend leagues. A leaderboard that refuses sybil farms.",
  icons: { icon: "/logo.svg" },
  openGraph: {
    title: "Sparkboard — play-money prediction markets",
    description: "Open a market. Trade Sparks. No cash-out. LMSR prices.",
    images: ["/mark.jpg"],
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${newsreader.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
